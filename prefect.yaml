name: semiconductor-pipeline
prefect-version: 3.8.3

build: null
push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /code

deployments:
  - name: gcp-elt
    entrypoint: src/flows/gcp_elt.py:gcp_elt_flow
    work_pool:
      name: semiconductor-pool
      work_queue_name: default
    schedules: []
